# VRCColliderProbe 仕様メモ

VRChat のワールド内を OSC で自動歩行し、**床コライダーの張り忘れ（床抜け）** を
見つけて地図付きで報告するオートプレイヤー。発想はルンバ。

> このメモは RelateAnything プロジェクト（`E:\AIProject\RelateAnything`）の
> セッションで方針を詰めた内容のまとめ。**まだ何も実装していない**。
> 「要検証」と書いた項目は推測・記憶ベースなので、最初に実機で確かめること。

---

## 1. 目的とスコープ

### やりたいこと

1. ワールドを自動で歩き回る
2. 壁やコライダーのある物体に阻まれたら、それを検知して旋回する
3. 落下を検知し、**床抜け（落ち続けてリスポーン）** なら地点を記録する
4. 歩いた軌跡・踏破範囲・床抜け地点を**地図**にまとめて報告する

### やらないこと（少なくとも最初は）

- 画像認識による本格的な障害物回避（まずは OSC の速度だけでやる）
- 公開インスタンスでの実行（→ 7. 運用上の注意）
- 完全な網羅保証（ランダム歩行ベースでは狭い穴を踏まない可能性がある）

### 補完関係にある別手段

自分のワールドなら、**Unity エディタで床を格子状にレイキャストして
「メッシュはあるがコライダーが無い場所」を洗い出す**ほうが確実で漏れがない。
このオートプレイヤーは「実際に歩いたとき抜けるか」を確かめる**実地テスト**という位置づけ。
両方やるなら、エディタ側の静的チェックを先にやる価値が高い。

---

## 2. 基本設計：ルンバとの対応

| ルンバ | このツール |
|---|---|
| 車輪の回転数で位置推定（オドメトリ） | OSC の速度を積算して位置推定（デッドレコニング） |
| バンパーで衝突検知 → 旋回 | 前進入力中なのに速度がほぼゼロ → 壁と判定して旋回 |
| 段差センサー | `Grounded` と `VelocityY` で落下検知 |
| 掃除済みエリアを記録 | 踏破した格子を地図に塗る → 未踏へ向かう |
| 充電台 | リスポーン地点（位置が既知の点） |

**画面解析なしで主要機能が成立する**のが要点。画面は「証拠の保存」に使う。

---

## 3. VRChat OSC（要検証）

VRChat の設定で OSC を有効にする（アクションメニュー → Options → OSC → Enabled）。

### 入力（こちら → VRChat、既定ポート 9000）

| アドレス | 型 | 用途 |
|---|---|---|
| `/input/Vertical` | float -1〜1 | 前後移動 |
| `/input/Horizontal` | float -1〜1 | 左右移動 |
| `/input/LookHorizontal` | float -1〜1 | 旋回 |
| `/input/MoveForward` 等 | int 0/1 | ボタン式の移動（float 版と選べる） |
| `/input/Jump` | int 0/1 | ジャンプ |
| `/input/Run` | int 0/1 | 走り |

注意: ボタン式（int）は **1 を送ったら 0 を送り返さないと押しっぱなし**になる。
終了時・例外時は必ず全入力を 0 に戻すこと（止め忘れると歩き続ける）。

### 出力（VRChat → こちら、既定ポート 9001）

アバターの組み込みパラメータが `/avatar/parameters/<名前>` で届く（要検証）。

| パラメータ | 用途 |
|---|---|
| `Grounded` | 接地しているか。落下検知の主軸 |
| `VelocityY` | 縦速度。落下の強さ |
| `VelocityX` / `VelocityZ` | 水平速度。**ワールド座標かアバター基準かを要検証** |
| `AngularY` | 旋回の角速度（向きの推定に使えるかも） |

### 実機で確認した結果（2026-09-21、デスクトップモード）

確認用スクリプト: `tools/osc_probe.py`（ログは `logs/`）

- 組み込みパラメータは `/avatar/parameters/<名前>` で届く。**値が変化したときだけ送られる**
  （一定速度で歩いている間や接地し続けている間は何も来ない）→ 受信側は最後の値を保持する。
  起動直後は `Grounded` などの初期値が分からない
- `VelocityX/Z` は**アバター基準**（前進で Z+、右移動で X+）。200° 旋回した後でも前進は Z+ のまま
- `VelocityX/Z` は平滑化されていて、止まると約 0.3 秒かけて 0 に戻る。
  `VelocityMagnitude` は実速度をすぐ反映する（停止時は即 0）→ **詰まりの判定は `VelocityMagnitude` で行う**
- 壁に当たったとき: `VelocityMagnitude` が 3.0 → 0.48 に急に落ち、壁に沿って横に滑る
- 歩行速度は 3.0 m/s（このワールドの場合）。入力から動き出すまで約 20ms
- 旋回: `LookHorizontal=0.5` では**旋回しない**。`1.0` や `/input/LookRight`=1 では **一定の 200°/s**
  （`AngularY=200`。値の変化時だけ届くので、200 を受けてから 0 を受けるまでの時間で角度が分かる）。
  画面キャプチャでも、1 秒で約 200° 回ったのを確認した
- ジャンプ: `Grounded=False` と `VelocityY=+3.84` が届き、そのあと `VelocityY` が 9.81 m/s² で減っていく。
  着地で `Grounded=True`・`VelocityY=0`
- VRChat のウィンドウが前面になくても OSC 入力は効く。画面キャプチャも、隠れていなければ撮れる
- 軸入力（float）は送った値が保持される。念のため 0.1 秒ごとに送り直している
- **リスポーン**（`logs/osc_20260921_140107.csv` の t=12.18）: 空中（`Grounded=False`）のまま
  `VelocityY` が -10.79 → -0.16 に一気に戻る。その後、スポーン地点で少し落ちて `Grounded=True` になる。
  このワールドではリスポーンまでの落下が約 1.1 秒・約 6m と短かった
- 落下の遷移パターンの見分け方:

  | 出来事 | `VelocityY` | `Grounded` |
  |---|---|---|
  | 着地（段差・ジャンプ後） | 負 → **ちょうど 0** | 同時に True |
  | ジャンプ | 0 → **+3.8 前後** | 同時に False |
  | **リスポーン（テレポート）** | 大きく負 → **ほぼ 0（-0.16 など）**。1 サンプルで +3 以上戻る | **False のまま** |

  重力では 1 サンプル（約 17ms）に 0.16 m/s しか変わらないので、「空中で `VelocityY` が急に戻った」ことが
  テレポートの確かな印になる。Udon のテレポートも同じに見えるはずなので、報告では「リスポーン／テレポート」として扱う
- 段差を降りるときは、`Grounded` が 17ms ほどの間隔で False/True を何度か繰り返すことがある → 短い空中時間は無視する
- 空中の水平速度は `VelocityZ=4.17` と地上（3.0）より速い値になっていた（慣性の影響かもしれない。デッドレコニングでは実速度として積分する）
- 未確認: VR モードでの挙動

### 取れないもの

- **現在位置（ワールド座標）は OSC で出てこない**（要検証だが、取れない前提で設計する）
- 向き（ヨー角）も直接は出ない。旋回は自分で入力しているので、入力と `AngularY` から推定する

---

## 4. 機能ごとの設計

### 4.1 壁・障害物の検知と旋回（バンパー）

- 前進入力中に、水平速度 `√(VX² + VZ²)` が一定時間しきい値未満 → 詰まり
- 旋回角はランダム（例: 90〜180°）。ルンバと同じく、ランダム性が網羅性を上げる
- 詰まった地点は地図に「壁」として記録（コライダー配置の確認にもなる）
- 旋回直後にまた詰まる場合（角にはまった）は、大きく旋回＋少し後退

### 4.2 落下の検知と分類

落ちたら全部報告すると、階段・段差・わざと作った吹き抜けまで引っかかる。

| 状況 | 判定の目安 | 扱い |
|---|---|---|
| 段差・階段 | `Grounded` が false になったが、短時間で true に戻った | 地図に記録するだけ |
| **床抜けの疑い** | false のまま `VelocityY` が大きく負の状態が続く → リスポーン | **報告対象** |

- リスポーンの検知: 長い落下のあと `Grounded` が true に戻る＋速度がリセットされる（要検証）
- 床抜け地点 = 最後に接地していた位置（落下開始直前の推定位置）

### 4.3 位置推定と地図（デッドレコニング）

- 水平速度を時間積分して位置を推定。アバター基準の速度なら、推定した向きで回転させてから積分
- **ズレは溜まる**が、**リスポーンで既知の点に戻る**ので無限には溜まらない
- 地図は格子（例: 0.5m 四方）で持つ: 未踏 / 踏破 / 壁 / 段差 / 床抜け
- リスポーン地点を原点にする

### 4.4 位置精度を上げる選択肢（後回しでよい）

自分のワールドなら、**テスト用ビルドにだけ Udon で座標を画面に表示**し、
それを画面キャプチャから読めばズレのない位置が取れる。
- 数字の OCR より、座標を色のブロック（簡易 2 次元コード）にして読むほうが堅い
- ワールド側に手が入るので、デッドレコニングで精度が足りないと分かってから検討

### 4.5 探索戦略

1. 最初はランダム歩行＋バンパー（ルンバ初期型）
2. 地図ができたら、未踏の格子に近い方向を優先（フロンティア探索）

### 4.6 画面キャプチャ（証拠）

- 常に直近数秒分のフレームをリングバッファに保持
- 床抜けを検知したら、落ちる直前の数枚を書き出して報告に添付
- 画面キャプチャのコードは RelateAnything の `tools/game_scene.py` から流用できる
  （`mss` + ウィンドウ追従 + DPI aware 化）→ 8. 流用できるもの

### 4.7 報告

- 軌跡・踏破範囲・壁・段差・床抜けを塗り分けた**地図画像**
- 床抜け地点ごとに、落下直前の画面キャプチャ
- 実行時間・歩行距離・踏破面積などの概要
- 形式は PNG + HTML（地図と画像を 1 ページに）あたりが扱いやすい

---

## 5. 進め方（段階と完了条件）

一度に全部作ると、どこで失敗しているか分からなくなるので、段階を踏む。

| 段階 | 内容 | 完了条件 |
|---|---|---|
| **1. OSC 疎通** | 受信した値を全部ログに出す。移動入力を送る | `Grounded`・`Velocity*` が届き、入力で実際にアバターが動く。**速度がワールド基準かアバター基準かが分かる** |
| **2. 反射行動** | 前進・詰まったら旋回・落下検知 | 壁で止まらず歩き続け、床抜けで落ちたら記録が残る |
| **3. 地図** | デッドレコニングで軌跡と落下地点を記録 | 軌跡を描くとワールドの形がそれなりに分かる |
| **4. 探索** | 未踏エリア優先 | ランダム歩行より踏破面積が増える |
| **5. 報告** | 地図＋画面キャプチャをまとめて出力 | 床抜け地点が一目で分かる |

### 段階 2 の結果（2026-09-21、`tools/walker.py`、`runs/20260921_140726`）

- 60 秒の実行で、詰まり → 旋回 9 回、段差 3 回、リスポーン 2 回。止まらずに歩き続けた
- リスポーン 2 回は、どちらも**島の外周から歩いて落ちたもの**（保存した画面で確認）。
  検知は正しいが、中身はコライダーの張り忘れではなく「ワールドの端」。
  → 外周の落下と本当の床抜けを分けるのが次の課題。段階 3 の地図で「踏破範囲の縁かどうか」を見るか、
  落ちる直前の画面で判断する（RelateAnything の出番になりうる）
- 壁際では三人称カメラが壁にめり込み、アバターの頭しか写らないフレームがある

### 段階 3 の途中で見つかった「はまり方」（2026-09-21）

- **家具の上で跳ね続ける**（`runs/20260921_141535`、ソファ）: `Grounded` が約 67ms ごとに False/True を繰り返し、
  `VelocityY` が +0.25〜-0.07 を往復。水平速度は 0。詰まり判定を `Grounded` ではなく
  水平速度（`√(VelocityMagnitude² − VelocityY²)`）で行うよう修正した
- **コライダーの隙間に挟まる**（`runs/20260921_141919`、クッションとアーチの間のランタンの上）:
  `Grounded=False` のまま `VelocityY` が重力どおり増え続け（100 秒で -992 m/s）、位置は変わらない。
  リスポーンもしない。落下状態が 3 秒続いたら「挟まり」として記録し、ジャンプ＋移動で抜け出す。
  `runs/20260921_142332` では「後退＋ジャンプ」の 1 回目で抜けた。
  **挟まる場所もコライダーの不具合として報告する価値がある**（地図に W で表示）
- 画面の動き（直近 2 フレームの輝度差）は、挟まり中 0.5 前後、何もない空間への落下 1.4〜2.6。
  背景が一様だと落下中も小さいので、挟まりの判定には使わず記録だけにした

### 段階 3 の結果（`runs/20260921_142514`、スポーン地点から 3 分）

- 歩行距離 445m、踏破面積 112m²、詰まり 21、段差 8、リスポーン 7（すべて外周と判定）、挟まり 0
  （踏破面積は mapper の補間修正後の値で 191m²。段階 4 の節を参照）
- 7 か所の落下地点がスポーン地点を中心とする半径 20〜30m の輪の上に並び、島の外周をなぞっている
- 別々の走行で同じ場所から落ちた 2 組（R1 と R5、R4 と R6）が 3〜4m 以内に集まった。
  スポーン北側の壁も毎回同じ位置に出る → 位置の推定は数 m の精度で安定している
- 課題: ランダム歩行は外周に向かって一直線に進みがちで、内側の踏破がまばら（段階 4 で対処）

### 段階 4（`tools/explore.py`、`walker.py --strategy frontier`。既定）

歩いた跡を幅 1.5m の帯で塗り、15° 刻みの向きごとに「幅 1.5m・長さ 1〜8m の通路にある未踏の格子」を数えて
最も多い向きへ進む。帯から 3m 以内の未踏は重く、それより遠い未知は軽く数える。直進中も 3 秒ごとに見直す。
リスポーンした落下地点は崖として記録し、2.5m 以内に近づいたら曲がる。

偽ワールド（半径 25m の島＋障害物、実測どおりの OSC を返すシミュレータ）で 4 分ずつ比べた結果:

| 方式 | 帯（幅 1.5m）の面積 | 足元の面積 |
|---|---|---|
| ランダム（段階 2） | 580 / 527 m² | 257 / 231 m² |
| 探索 | 718 / 736 m²（+24% / +40%） | 269 / 268 m² |

途中で分かったこと:
- 軌跡の真横を重く数えると、自分の軌跡に沿って引き返すだけになった
- 未踏を一律に数えると、リスポーン地点から毎回外へ一直線に進んで落ちるだけになった（ランダムより 3 割少ない）
- **値は変化時しか届かないので、直進中はメッセージが来ない。位置の推定をメッセージ受信時にしか進めていないと、
  直進した区間が丸ごと抜ける。** walker は毎ループ現在時刻まで積分し、mapper は 0.1 秒刻みで補うよう修正。
  これで `runs/20260921_142514` の踏破面積は 112 → 191 m² になった（実機は値が細かく揺れて頻繁に届くので目立たなかった）
- リスポーンの確定: 空中で `VelocityY` が急に戻った値が 0 でなければ、着地を待たずに確定する
  （スポーン地点が床の高さだと、テレポート直後すぐ着地して「着地」と誤判定する）

実機（このワールド、スポーン地点から 3 分）:

| 走行 | 足元の面積 | 帯の面積 | 歩行距離 |
|---|---|---|---|
| ランダム `runs/20260921_142514` | 191 m² | 516 m² | 445 m |
| 探索・初回 `runs/20260921_150122` | 121 m² | 326 m² | 348 m |
| **探索・修正後** `runs/20260921_150810` | **214 m²（+12%）** | **610 m²（+18%）** | 448 m |

- 初回は、斜めの壁に沿って滑って同じ角に運ばれる往復を 60 秒続けた。壁を「詰まった点の正面 1 マス」しか
  記録していなかったので、滑った壁が地図に載らず、その向きがずっと「未踏が多い」ように見えていた。
  偽ワールドの障害物は四角い箱だけだったので再現しなかった
- 修正: 壁沿いに滑っている間（向きと動く方向が 30° 以上ずれる）も正面を壁として記録する、壁を幅 1.5m で記録する、
  60 秒以内に同じ場所で 3 回詰まったらランダムに曲がる
- → 段階 4 の完了条件（ランダムより踏破面積が増える）は満たした。ただし 1 回ずつの比較なので差は参考値
- リスポーンのたびにスポーン地点に戻されるので、軌跡はスポーン地点から放射状になりやすい。
  外周に近い場所ほど踏みにくい

### 段階 5（`tools/report.py`。walker 終了時に自動実行）

`runs/<日時>/report.html` に 1 ファイルでまとめる（地図・画面キャプチャは base64 で埋め込み。3 分の走行で約 1.3 MB）。
- 冒頭: 結論の一文（床抜けの疑いがあるか）、判定ごとの件数、実行時間・歩行距離・踏破面積など
- 要確認の地点: 床抜けの疑い → 挟まり → 判定保留の順。地点ごとに周辺 16m 四方の地図（落ちた向きの矢印つき）と直前 4 枚の画面
- 全体の地図と凡例
- 外周の可能性が高い落下は折りたたみ
- 挟まりは、スポーン地点以外から始めた区間で位置が出せなくても要確認に載せる（画面で場所が分かるため）
- ライト／ダーク両対応、スマホ幅でも崩れない
- 既存の走行にも `uv run tools/report.py runs/<日時>` で作れる。`.claude/launch.json` の `reports` は runs/ を 8791 番で配信する（プレビュー確認用）

### 拡張（2026-09-21）

**RelateAnything の注釈**（`tools/scene_annotate.py` を RelateAnything の venv で別プロセス実行。結果は `scene.json` にキャッシュ）
- 報告の各地点で、落ち始めの 0.4 秒前の画面に検出枠を描き、「アバター は ledge の上にいる」のような位置関係を載せる
- 何でも拾う検出器（4585 種類）は雑音が多かった（髪 → umbrella、画面全体 → studio shot）。
  床・縁・階段・家具など 41 語に絞り、確信度のしきい値を 0.1 に下げた（0.25 だとほぼ person しか残らない）
- 関係は位置関係（上・中・近く・前後など）だけにし、アバターが関わるものがあればそれだけを載せる
- 報告に載せる画面を「リスポーン直前」から「落ち始めの前後」に変更（直前の 4 枚は全部落下中で何も写っていなかった）

**高さ（3D）**
- `VelocityY` を常に積分して高さを出す（接地中も坂では ±1.2 m/s ほど届く）。リスポーンで 0 に戻す
- 別々の区間で同じ 1m 格子を通ったときの高さの差: 37 か所で中央値 0.21m、89% が 0.5m 以内、最大 1.9m
- 段差を上るときは縦速度が届かないことがあり、低めに出る可能性がある
- 2D 地図の踏破格子を高さで色分け。報告に canvas の 3D ビュー（ドラッグで回転、高さの強調）を追加

**リアルタイムのオーバーレイ**（`tools/overlay.py`、`walker.py --overlay`）
- VRChat の画面の右上に、最前面・クリックスルー・半透明のミニマップ（北が上、半径 15m）を重ねる
- 当初は `SetWindowDisplayAffinity(WDA_EXCLUDEFROMCAPTURE)` で証拠用の画面（mss）から除外していたが、
  これだと OBS や Win+Shift+R の録画にも写らなかった
  → 画面の撮影を **PrintWindow（`PW_CLIENTONLY | PW_RENDERFULLCONTENT`、ウィンドウの中身だけ）** に切り替え、
  オーバーレイの除外をやめた。VRChat（Unity の DirectX 描画）でも撮れ、1 枚 約 34ms。
  上に重なったオーバーレイや別のウィンドウは写らない。画面を撮る方法（録画ソフトと同じ）ではオーバーレイが写ることを確認した。
  以前の動作に戻すときは `--hide-overlay-from-recording`
- OBS では「画面キャプチャ」で録る（「ゲームキャプチャ」「ウィンドウキャプチャ」は VRChat のウィンドウだけを撮るので、
  別ウィンドウのオーバーレイは写らない）
- 大きさは `--overlay-size`（既定 520px。300 → 400 → 520 と大きくした）

**RelateAnything のリアルタイムのオーバーレイ**（`tools/relate_overlay.py`、`walker.py --relate-overlay`）
- RelateAnything の venv で別プロセス。VRChat の画面を撮って推論し、検出枠を画面全体に、状況説明を左下に重ねる
- 黒をカラーキーにした透明ウィンドウ。キャプチャから除外するので、証拠用の画面にも推論用の画面にも写らない
- 推論は約 8 回/秒まで出るが、VRChat と GPU を取り合うので既定で 4 回/秒に抑える（`--fps`）
- ポスターの人物・鏡に映った自分・アバター一覧のサムネイルも person になる（実機で 5 つ）。
  確信度 × 画面中央への近さで自分のアバターを 1 つ選び、他の person が絡む関係は説明に使わない

**どこに何があるか**（`tools/semantic.py`。報告を作るときに実行、`semantic.json`）
- walker が走行中の画面を 1 秒に 1 枚 `frames/` に保存する（`--frame-log`、0 で無効）。解析は報告を作るとき
  （走行中に GPU を使わず、同じデータで何度でも調整し直せるように）
- 各画面の撮影時刻から、アバターの推定位置と向きを引く（mapper の軌跡に時刻と向きを追加した）
- アバターが主語の関係だけを使う。「上にいる／立っている」→ 足元（その場所の床の種類）、
  「近く／隣／前／後ろ」→ 画面上で写っている方角へ 1.5m ずらした位置（横の視野角は 80° と仮定）
- 同じ種類で 2.5〜3m 以内のものをまとめ、1 枚でしか見えていない弱い検出は捨てる
- 報告に、名前を重ねた地図・一覧表（代表の画面つき）・3D ビューのラベルを追加
- 初回の試走（`runs/20260921_163834`）: 152 枚中 123 枚が使え、観測 44 件 → 17 件。ほとんどがスポーン地点
  付近の「床」「台・足場」で、1 枚だけの検出が多い。この走行は後半 75 秒閉じ込められていたので、次の試走で再評価する
- 2 回目の試走（`runs/20260921_164557`、修正後）: 151 枚 → 観測 83 → 20 件。11 件が「床」（スポーン地点の床は
  14 枚でまとまった）。「プール（足元、スポーン地点の右 10m）」は実際のプールの位置と合う。「テーブルの上」など誤りもある。
  アバターと関係が付く近くの物しか拾えないので、情報量は少ない → 段階 2（三角測量）が本命
- 同じ試走で、挟まり 2 回はどちらも 1 回目の抜け出しで正しく抜けた（0.5 秒の接地を確認）。閉じ込めはなし。
  歩行距離 415m・踏破面積 192m² で通常の水準
- 「上にいる」を足元として扱うのは床らしいもの（`FLOOR_LABELS`）だけにした。テーブル・ベンチなどは「近く」
- **ミニマップへの表示**: relate_overlay.py が見つけた関係（アバター基準の方角つき）を `live_obs.jsonl` に追記し、
  walker.py が撮影時刻（エポック秒）に最も近いアバターの位置に置いてまとめる（`semantic.LiveLandmarks`）。
  3 回以上見えた、または確信度 0.5 以上のものだけを表示し、最後に見えてから 2 分（`--landmark-ttl`）で消す。
  古いほど薄く描く。床・地面はどこにでもあって目印にならないので、ミニマップには出さない
- **報告にもリアルタイムの観測を取り込む**（`runs/20260921_165505`）: リアルタイム側は毎秒約 4 回推論するので、
  3 分で 535 件（ランプ 38・看板 33・植物 32・壁 29・ベンチ 28・ポータル 26 など）と、1 秒ごとの画面の解析
  （観測 96 件）よりずっと多い。両方を合わせてまとめると 20 件 → 129 件になり、ワールドの様子が読める地図になった。
  「ポータル（スポーン地点の右 11m、8 回）」は実際にポータルの前まで歩いた方向と合う
- 時刻合わせ: walker の start イベントに `epoch0`（ログの時刻 0 に当たるエポック秒）を記録する。
  記録のない走行は、frames/ の画面ファイルの更新時刻とファイル名の時刻の差から推定する
- 段階 2（三角測量で見えている物の位置を出す）はその後

### まとめての試走で見つかったこと（`runs/20260921_163834`、両方のオーバーレイ付き）

- **RelateAnything のオーバーレイで VRChat が重くなった形跡はない**（OSC の間隔の中央値は全区間で 16.7ms）
- 挟まりから「抜けた」と早合点していた: 後退＋ジャンプの直後に 50ms だけ接地し、また落下状態に戻る。
  3.3 秒ごとに挟まりの検知を繰り返した → 0.5 秒続けて接地したら抜けた、に変更
- **閉じ込め**: 外周の岩壁を落下姿勢で滑り降り（段差 5 回で約 16m 下）、岩の隙間に入り込んで四方に動けなくなった。
  接地したままなので挟まり（落下状態）の検知に掛からず、75 秒間ループの断ち切りを繰り返した
  → 40 秒以内に同じ場所（2m 以内）でループの断ち切りが 3 回起きたら閉じ込めとして記録し、抜け出しを試す。
  抜けられなければ停止。報告では挟まりと同じ扱い（位置は閉じ込められた地点）
- **同じ隙間への挟まりの繰り返し**（`runs/20260921_170901`）: スポーン地点近くの「Use WASD to move」看板の奥、
  2 本の岩の柱の間。後退＋ジャンプで抜けた後、同じ向きのまま前進を再開して数歩で戻り、約 4 秒ごとに 16 回挟まった
  → 抜けたら挟まった場所を崖として記録し（以後 2.5m 手前で曲がる）、今の向きから ±120° を避けて向きを変えてから歩く。
  報告では 2m 以内の挟まりを 1 件にまとめ、回数と時刻を載せる
- **ジャンプで乗り越える**（`--no-climb` で無効）: 壁に詰まったら、向きを変える前に 1 回だけ前進＋ジャンプを試す。
  1 秒後（着地後）にジャンプ前の位置から向きの方向へ 0.8m 以上進めていれば乗り越えた、とみなして前進を続ける。
  跳べる高さは約 0.75m（初速 3.84 m/s）。外周の柵を越えて落ちないよう、既知の崖（落下地点・挟まった場所）から
  3m 以内では跳ばない。越えられなかった壁には二度跳ばない。地図には緑の▲。自分のジャンプの着地は段差に数えない。
  偽ワールド（高さ 0.5m の低い壁を追加）では、低い壁は 2 回とも越え、高い箱は 3 回とも失敗と判定した。
  RelateAnything で正面の手すり・柵を見て跳ばない仕組みは、実機の様子を見てから検討
- 実機（`runs/20260921_173334`）: 16 回試して 6 回成功（うち 3 回は高さ +0.6〜0.7m、一段高い台に乗った）、失敗 9 回。
  **1 回は越えた直後に外周から落ちた**（まだ崖と分かっていなかった場所）。1 回は跳んだ先で挟まり、3 つ目の手で抜けた。
  歩行距離 414m・踏破面積 191m² で効率は従来と同程度。段差の数は自分のジャンプの着地を除いて 20 → 10
  → 一度落ちた場所の 3m 以内では以後跳ばないが、初めての外周では起こりうる。対策案: 跳ぶ直前の画面に
  railing / fence が写っていたら跳ばない（RelateAnything）
- **ワールドごとの記録（続きから）**（`tools/world.py`、`tools/knowledge.py`、`worlds/<ID>/knowledge.json`）:
  VRChat のログ（`%LOCALAPPDATA%Low\VRChat\VRChat\output_log_*.txt` の `Joining wrld_…` と `Entering Room:`）から
  ワールド ID と名前を取る。走行ごとの寄与（歩いた格子・壁・崖・落下と判定・挟まり・乗り越え・見つけたもの）を分けて保存し、
  読み込むときに合算する（取り込み直しても二重に数えない）。walker は開始時に読み込み、歩いていない場所を優先し、
  過去に落ちた・挟まった場所を最初から避ける。報告に「このワールドの累計」の節（全走行を重ねた地図・走行一覧）。
  過去の 14 走行を取り込んで、VRChat Home の累計の踏破は 969m²
- **遠くの未踏エリアを目標にする**（`explore.frontier_targets`、`--no-goal` で無効）: 記録を読み込んだ走行でも、
  新しい場所の割合は 32%（記録なしの走行の 29〜40% と同程度）だった。近くの判断は 8m 先までしか見ないので、
  スポーン地点の周りを歩き尽くすとどの向きも点数がほぼ 0 になり、実質ランダムになっていた。
  → 地図全体から「歩いた帯に接する未踏のまとまり（6 マス以上、崖から 2m 以上）」を探し、大きさ ÷ (1 + 距離/15m) が
  最大のものを目標にする（まとまりの中で今の位置に一番近い格子）。向きの点数に目標の方角への加点（最大 12）を足す。
  2.5m まで近づいたら次の目標、40 秒近づけなければあきらめて付近を避ける。ミニマップに目標（円、範囲外なら縁の矢印）を表示。
  偽ワールドで「1 回目の記録を読み込んだ 2 回目」を比べると、新しい場所の割合は目標あり 87% / なし 82%（目標 8 回中 7 回到達）
- **30 分の走行**（`runs/20260921_180522`、両方のオーバーレイ・記録リセット直後）: 歩行 3922m、踏破 1049m²
  （3 分の走行の約 5 倍。島全体を歩き尽くし、落下地点が外周を縁取る地図になった）、リスポーン 71、挟まり 4（すべて脱出）、
  ジャンプで越えた 22、目標 44 回設定・41 回到達、見つけたもの 718 件（1 秒ごとの画面 1580 枚＋リアルタイム観測 4637 件）。
  見つかった問題と対応:
  - 床抜けの疑いが 9 件出たが、画面で確かめるとすべて外周。71 区間の軌跡を全部重ねて判定していたため
    → 判定に使う床を「落ちた区間と前後 2 区間」に限った（`mapper.TRACK_WINDOW`）
  - 高さの異常（-53m）: 挟まりの途中の 50ms の接地で「最後に接地した高さ」が下がりきった値に更新されていた
    → 0.2 秒未満の接地は数えない。また、壁に擦れながら落下状態で滑り降りると VelocityY が自由落下のように増える
    → リスポーンせずに着地した落差を、リスポーンまでの落差の 75% 点で打ち止め。最低 -16.9m（外周の岩場）に収まった
  - 報告が 18MB → 外周の落下は画面 2 枚・横 420px にして 12.8MB
  - 上の修正後も「床抜けの疑い」2・「判定保留」2 が残ったが、画面ではすべて外周だった。
    **位置だけの判定は長い走行では誤判定が混ざる。要確認の地点は画面で確かめる前提**。
    改善案: 落ち始めの画面で、アバターの先が空（紫の空間）かどうかを見る（RelateAnything の sky / edge など）
- **簡易 GUI**（`tools/gui.py`）: ブラウザで開くローカル専用の画面（127.0.0.1:8790、標準ライブラリのみ、
  POST は Origin で他サイトからの操作を弾く）。走行の開始・停止（時間・オーバーレイ・RelateAnything・スポーン以外から・
  乗り越えなし・--fresh）、走行中の状況とログ、今いるワールドの累計の地図、走行の一覧と報告へのリンク、記録のリセット（退避）。
  停止は walker の `--stop-file`（全入力を 0 に戻して報告まで作る）。子プロセスは `-u` で起動しないとログが届かない
- 2026-09-21 17:49 に VRChat Home の記録をリセットした（一部おかしくなっていたため）。
  以前の記録は `worlds/wrld_4432ea9b-…/knowledge_backup_20260921_174946.json`
- 報告に、累計の「見つけたもの」の表（見えた走行数・回数、上位 80 件）を追加。1 回の走行の表も上限を 80 件に
- **累計の床で床抜けを判定するのはやめた**: 位置の推定は走行ごとに数 m ずれるので、他の走行の軌跡が外周の外側に来て、
  外周の落下が「周りを床に囲まれている」ように見えた（累計の地図に床抜けの疑いが 25 か所出た）。
  判定は各走行の中で行い、累計はその結果を載せるだけ。累計で判定するには走行どうしの位置合わせ
  （壁や見つけたものを手がかりにずれを補正する）が要る
- 挟まっている間も VelocityY は増え続けるので、高さが -108m のような値になっていた
  → 挟まりの検知時・脱出時に、挟まる直前の接地の高さに戻す。検知までの 3 秒間の軌跡もさかのぼって直す

段階 1〜2 だけでも「落ちる場所を見つける」という本来の目的は果たせる。
地図はそれを分かりやすくするための上乗せ。

### 最初にやること

1. VRChat で OSC を有効にし、自分だけのインスタンス（または Build & Test）に入る
2. 9001 を受信して、届くアドレスと値を全部表示するスクリプトを書く
3. 歩く・止まる・ジャンプする・段差から落ちる、をやって各値の変化を確認する
4. その結果を見て、このメモの「要検証」を埋める

---

## 6. 技術スタック案

- Python 3.12+（`uv` で venv 管理。RelateAnything と同じ流儀）
- `python-osc` … OSC 送受信
- `mss` … 画面キャプチャ
- `pywin32` … ウィンドウ特定（VRChat のウィンドウに追従）
- `numpy` / `opencv-python` … 地図の描画・画像保存
- torch 等の重い依存は不要

---

## 7. 運用上の注意

- 自動操作は**プライベート／招待制インスタンス、または Build & Test のローカル**で行う。
  公開インスタンスで無人のアバターが歩き回るのは他の人の迷惑になりうる
- 入力の止め忘れ防止: Ctrl+C・例外・終了時に必ず全入力を 0 に戻す
- VRChat のウィンドウが非アクティブでも OSC 入力は効く（確認済み）
- ワールド内のポータルは、VRChat の設定で「追加の操作をしないと入らない」ようにできる。
  自動歩行の前に設定しておく（歩いて触れただけで別ワールドに飛ぶのを防ぐ）
- 念のため `tools/walker.py` は `/avatar/change` を受けたら即停止する（ワールド移動で届くかは未確認）

---

## 8. RelateAnything から流用できるもの

`E:\AIProject\RelateAnything\tools\game_scene.py` に以下がある。

- `enable_dpi_awareness()` … 物理ピクセル座標で扱うための DPI 対応
- `list_windows()` / `find_window()` / `client_rect()` … タイトル部分一致でウィンドウ特定
- `ScreenSource` … `mss` でウィンドウのクライアント領域を毎フレーム追従して取得

注意: `mss` は**画面上の領域**を撮るので、VRChat の上に別ウィンドウが重なると
そちらが写る。画面キャプチャを証拠にする以上、VRChat は前面に出しておくこと。

---

## 9. Windows で踏んだ地雷（RelateAnything での経験）

- **`.bat` に日本語を書かない**。cmd は Shift-JIS で読むので、UTF-8 の日本語が化けた
  バイト列に `&` 相当が混ざり、コメント行の途中がコマンドとして実行された
- コンソールは cp932。Python の出力に cp932 で表せない文字（`—` など）があると落ちる
  → `sys.stdout.reconfigure(errors="replace")` で回避
- `cv2.imwrite` / `cv2.imread` は日本語パスを扱えない → `cv2.imencode(...).tofile()` / `np.fromfile` + `cv2.imdecode`
- Git Bash の heredoc 経由で日本語を含む Python を流すと、文字列が一致しないことがあった
  → 日本語を含むパッチはファイルに書いてから実行するか、エディタで直接編集する

---

## 10. 未決事項

- ~~`VelocityX/Z` はワールド基準かアバター基準か~~ → アバター基準（3 章）。向きは `AngularY` の積分で持つ
- ~~リスポーンを確実に見分けるシグナルは何か~~ → 空中での `VelocityY` の急な戻り（3 章）
- 走り（`/input/Run`）で探索するか歩きでするか（速いほど狭い穴を飛び越えやすい）
- 位置精度が足りない場合、Udon の座標表示を入れるか
